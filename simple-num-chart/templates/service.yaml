apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-num-chart.fullname" . }}
  labels:
    {{- include "simple-num-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-num-chart.selectorLabels" . | nindent 4 }}
