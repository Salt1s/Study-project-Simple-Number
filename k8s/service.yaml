apiVersion: v1
kind: Service
metadata:
    name: simple-num-service
spec:
    selector:
        app: simple-num
    ports:
        -   protocol: TCP
            port: 80
            targetPort: 8080
    type: NodePort
